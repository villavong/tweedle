<%= simple_form_for([@post, @post.comments.build]) do |f| %>
     <%= f.input :comment ,label: "#{@post.comments.count} Comments"%>
     <%= f.submit "Create Comment", class: "button_highlight" %>
<% end %>
